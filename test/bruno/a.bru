meta {
  name: a
  type: http
  seq: 2
}

post {
  url: http://localhost:9005/api/posts
  body: json
  auth: none
}

body:json {
  {
    "count": {{total}},
    "saludo": "{{item}}"
  }
}

body:text {
  hola!
}

body:xml {
  <init>
      <auth>none</auth>
  </init>
}

vars:pre-request {
  total: 100
  item: item:aabbcc
}
