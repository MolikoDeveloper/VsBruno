meta {
  name: a
  type: http
  seq: 2
}

post {
  url: http://localhost:9005/api/posts
  body: json
  auth: none
}

body:json {
  {
    "count": "{{ammount}}",
    "saludo": "{{greet}}"
  }
}

body:text {
  hola!
}

body:xml {
  <init>
      <auth>none</auth>
  </init>
}

vars:pre-request {
  name: AlbertoSaurus
}

script:pre-request {
  console.log("hola!")
}
